# Binaries for programs and plugins
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

coverage.html

coverage.json

bsrouter/build
bsrouter/bsrouter.key
bsrouter/bsrouter.pem
